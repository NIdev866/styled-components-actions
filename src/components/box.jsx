import styled from 'styled-components';

const Box = styled.div`
  border: 1px solid white;
  height: 50%;
  width: 50%;
  display: flex;
  align-items: center;
  position: relative;
`

export default Box;
